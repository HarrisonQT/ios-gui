import React from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

const IconStyles = styled.div``;
const Icon = props => <IconStyles></IconStyles>;

Icon.propTypes = {};

export default Icon;
